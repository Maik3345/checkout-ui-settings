{
  "name": "checkout-ui-settings",
  "scripts": {
    "postinstall": "npx husky install",
    "dev": "cd preact && npm run dev",
    "build": "cd preact && npm run build",
    "build:watch": "cd preact && npm run build:watch"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.2.0",
    "@commitlint/config-conventional": "^18.1.0",
    "husky": "^8.0.3"
  }
}
