{
  "name": "checkout-ui-settings-preact",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "cd preact && npm run build",
    "build:watch": "cd preact && npm run build:watch",
    "prepare": "npx husky && cd preact && yarn",
    "test:cov": "cd preact && npm run test:cov",
    "format": "cd preact && npm run format"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.5",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
