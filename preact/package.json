{
  "name": "checkout-ui-settings",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:watch": "tsc && vite build --watch --emptyOutDir",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "test:ui:cov": "vitest --ui --coverage",
    "test:ui:cover": "vitest --ui --coverage",
    "lint": "biome lint ./src",
    "format": "biome format --write ./src",
    "prepare": "npx husky"
  },
  "dependencies": {
    "jotai": "^2.12.5",
    "preact": "^10.16.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.5",
    "@preact/preset-vite": "^2.10.1",
    "@testing-library/preact": "^3.2.4",
    "@types/jquery": "^3.5.32",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "happy-dom": "^18.0.1",
    "react": "^19.1.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
